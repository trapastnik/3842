# mtk38-globe — только шрифты

Прототип V1 «сфера» удалён 2026-08-02 при сборке финальной версии МТК 38.

**Папка остаётся ради `fonts/`** — это брендовые шрифты всего проекта
(Nolde, 21 Cent, 20 Kopeek). На них ссылаются ~50 файлов, из них 19 — в чужих
МТК (39–42), плюс координаторские `assets/shared/hub/hub.css` и корневой
`index.html`. Путь `../mtk38-globe/fonts/…` закреплён в COORDINATION.md как
общий, поэтому переносить каталог в одиночку нельзя.

Перенос в `assets/shared/fonts/` — задача координатора: нужен одновременный
проход по всем МТК.
