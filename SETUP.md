# SETUP.md — подключение новой Claude-сессии к проекту

Редакция 2026-08-03 (полная замена майской — правила менялись). Прочитал — сверься с
[COORDINATION.md](COORDINATION.md) «Хронология слияний» за последние даты: там всё новое.

## Шаг 1. Кто ты

Спроси пользователя, какой МТК ведёшь, и займи своё место:

| МТК | Тема | Worktree (`.claude/worktrees/…`) | Ветка | Dev-порт |
|---|---|---|---|---|
| 38 | Ленин на языках мира | `mtk38-language` | `claude/mtk38` | 8093 |
| 39 | Имени Ленина | `mtk39-globe` | `claude/bold-booth-f49a93` | 8096 |
| 40 | Библиотека Ленина | `mtk40-library` | `claude/flamboyant-cartwright-839223` | 8095 |
| 41 | Скульптуры | `mtk41-sculptures` | `claude/reverent-chandrasekhar-3484a8` | 8097 |
| 42 | Осмысление наследия | `mtk42-heritage` | `claude/xenodochial-goodall-8fcd6a` | 8098 |
| платформа | киоск-ядро (assets/shared/kiosk) | `kiosk-core` | `claude/kiosk-core` | 8091 |

Первое действие всегда: `git merge main` в своём worktree (подтянуть общий свежак).

## Шаг 2. Файловые границы

**Твоё:** `mtkXX-*/` (прототипы), `mtkXX-app/` (киоск-приложение), `data/mtkXX*.json`,
`assets/mtkXX/` (контент; исходники заказчика — в `assets/mtkXX/sources/`), своя секция README.

**НЕ трогай:** `mtkXX/index.html` (хаб-страницы — координатор), корневой `index.html`
(координатор), `assets/shared/**` (хаб, киоск-ядро, шрифты, projection.js — координатор и
сессия kiosk-core), чужие `mtkYY-*`, `PLAN*.md`, `SETUP.md`, `CLAUDE.md`.

**Append-only:** `COORDINATION.md` — дописывай в «Хронологию слияний», не переписывай чужое.

## Шаг 3. Git — жёсткие правила

- Коммить только в свою ветку, маленькими коммитами с внятными сообщениями.
- **Merge в main — ТОЛЬКО координатор.** Готовый кусок → скажи «готово к merge»
  (пользователю или сообщением координатору — см. Шаг 6). Сам в main не мержишь.
- **`git push` — НИКОГДА.** **VPS (`ssh ostrov`) — НИКОГДА, даже чтение.** Деплой — координатор.
- После координаторского merge: `git merge main` — забери результат.

## Шаг 4. Серверы

- Общий **8092** (корень репо, отдаёт main) — только координатор, не трогать.
- Свою незакоммиченную работу смотри на **своём dev-порту** (таблица выше) из своего
  worktree: поднял → проверил → погасил. 8094 не занимать (прод VPS).

## Шаг 5. Стандарты (канон — читать обязательно)

1. **[PLAN-KIOSK.md](PLAN-KIOSK.md)** — киоск-платформа: каждый МТК = приложение `mtkXX-app/`
   на ядре `assets/shared/kiosk/` (сцены с контрактом mount/unmount/pause/resume/reset/
   setLang/setA11y + декларативная схема настроек). Эталон — `mtk42-app/`.
2. **COORDINATION → «Тач-стандарт киоска» (пп. 1–7) и «Киоск-стандарт v2» (пп. 8–14)** —
   тач-таргеты 64/96px, отступы от кромки 64/80px, видимые скроллы, KioskHint, кегли 24/32,
   карты только вектор (канон `MtkProjection.WinkelTripel` + ne_110m), standby/idle, RU/EN/ZH,
   режим слабовидящих, 0 сети в рантайме.
3. **`assets/shared/kiosk/SETTINGS-INVENTORY.md`** — опись настроек твоих прототипов:
   при миграции каждая строка класса А переезжает в схему или явно снимается с причиной.
4. Проект в **горизонтали 3840×2160**; вертикаль отменена (2026-07-22).
5. Библиотеки — только вендоренные (офлайн-киоск, никаких CDN). Текст-текстуры мерить по
   actualBoundingBox. Рендер-буфер ≤ 8.3 Мп.
6. Приёмка своей работы: `KioskSelfTest.run(app)` (tools/selftest.js) на переднем плане +
   `tools/audit-offline.py --path mtkXX-app/`.

## Шаг 6. Связь (два канала)

- **Большое/долгоживущее** (правила, заявки с историей) — md в репо: Хронология COORDINATION.
- **Малое** (вопросы, «готово к merge», короткие заявки) — сообщение координатору:
  `mcp__ccd_session_mgmt__send_message` (сессию-координатора найдёшь в `list_sessions`).
- Пользователя дёргай только там, где нужно его решение.

## Шаг 7. Чек-лист первого захода

1. `git merge main` → прочитать Хронологию COORDINATION за последние даты.
2. Доложить состояние своей зоны (что в main, что в ветке, есть ли грязь).
3. Ждать/уточнить задачу. Не начинать крупное без задачи от пользователя или координатора.
