{
  "_note": "Фикстура регрессии: конфиг в формате 1.6.0, где состав экранов лежал в scenes.order/enabled. Ядро обязано перенести его в screens ДО наложения правок оператора — иначе файл откатывал бы их на каждом старте. Сюда же намеренно положены настройки сцен с id «order» и «enabled»: их нельзя ни принять за состав, ни удалить.",
  "defaultScene": "pulse",
  "scenes": {
    "order": ["cards", "grid", "pulse"],
    "enabled": { "grid": false },
    "pulse": { "rings": 11 },
    "order_scene_probe": { "keep": 1 }
  },
  "nav": { "size": 104 }
}
